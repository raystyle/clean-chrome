# MISTAKES：错误库

> 单文件起步（合法起点，错误族多了再拆 M1xx 分类文件）。一行一事，当场追加。

| 编号 | 现象 | 根因 | 正确处理 | 首踩日期 |
| --- | --- | --- | --- | --- |
| M001 | PowerShell here-string 拼补丁文本，插入内容与锚行粘连、末尾空行丢失，diff 出现重复函数签名 | here-string 收尾换行是闭引号终止符不属内容，首行空行才算真换行 | 多行插入块用显式 `` `n `` 拼接，不用 here-string；产出必须先 diff 再采用 [实证: 2026-09-11 补丁脚本两次返工] | 2026-09-11 |
| M002 | `$t.Replace([char]0x2192,'->')` 抛 MethodException 且非终止，后续语句带旧值继续跑，打印 done 假成功 | 字符串长度 2 匹配到了 char 重载 | 多字符替换写 `.Replace([string][char]0x2192,'->')` 或直接字面量；脚本加 `$ErrorActionPreference='Stop'` | 2026-09-11 |
| M003 | `git diff --no-index` 传 Windows 反斜杠路径，输出头带引号且路径含 `\`，后处理正则替换全部落空，patch 0 字节/脏头 | git 对含反斜杠路径按需加引号转义 | 传参一律用正斜杠；生成后 `Select-String '^diff --git'` 自检再落盘 | 2026-09-11 |
| M004 | `git apply` 后文件哈希与脚本输出不一致，疑补丁错误 | 本机全局 `core.autocrlf=true` 令 git apply 全文重写为 CRLF；内容实为等价 | Chromium 仓必须 `core.autocrlf false`；字节比对前先归一换行；真正判据是 `git apply --check` 通过 + 内容等价 [实证: 2026-09-11] | 2026-09-11 |
| M005 | 构建启动阶段 E: 盘反复掉线：fetch 中途目录消失、VS 安装器下载后「文件损坏且无法读取」，`Get-Volume` 显示 `Full Repair Needed` | E: 是外置 USB SSD（exFAT，无日志）：小文件高压写入触发文件系统脏位与瞬时掉线；Chromium 官方要求 NTFS 盘 | Chromium checkout 必须放内置 NTFS 盘（官方推荐 `C:\src\chromium` 一类根下短路径）；外置盘只做备份不做构建盘；开工前 `Get-Volume` 查 HealthStatus，迁移后重新下载曾落该盘的安装文件 [实证: 2026-09-11] | 2026-09-11 |
| M006 | 重启 fetch/clone 报 `destination path 'src' already exists`、fetch 报 `already contain a checkout`、或 `Unable to create .git/shallow.lock: File exists`；后台任务停了字节却还在涨或反之 | 停 fetch/gclient 任务只杀了 cmd 外壳：git 子进程、`src\.git\shallow.lock`、半成品 `src\`、`_gclient_*`、手写 `.gclient` 均残留；fetch 要求空目录且失败不续传（crbug.com/230691）；浅 fetch 启动即持 shallow.lock,被杀不释放 | 停任务后必须清场：`Stop-Process git,git-remote-https`，删 `src`、`_gclient_*`、`.gclient`、`src\.git\shallow.lock`，确认目录仅剩预期项再重启；启动后先读任务输出排除秒错，再采样字节判流 [实证: 2026-09-11 四连返工] | 2026-09-11 |
| M007 | 国内直连 googlesource/GitHub 时好时坏：同源 40 秒 18Mbps 与 80 秒 0 字节并存；git 卡死默认无限挂起；Gitee `mirrors/chromium` 镜像已 404（网传教程过时） | 传输层间歇阻断，非 DNS/握手问题；git 无低速超时默认值 | `GIT_CONFIG_COUNT` 环境变量注入 `http.lowSpeedLimit=1000`+`http.lowSpeedTime=90`（仅本进程树，勿动全局）；把「清场、fetch、失败重试」写成循环脚本挂后台；镜像优先 GitHub 官方只读仓 `chromium/chromium`（tag 齐全但直连同样抽风） [实证: 2026-09-11] | 2026-09-11 |
| M008 | vpython venv 构建炸 `failed to build package: python_venv-*`，gclient sync 每轮必死于此 | 用户级 `C:\Users\<u>\AppData\Roaming\pip\pip.ini` 带 UTF-8 BOM，pip 读配置在第一行就解析失败（`'\ufeff[global]'`）；vpython 的 pip install 未被 `--isolated` 豁免 | pip.ini 用无 BOM UTF-8 重写（内容原样保留）；同理任何被 pip/pip-adjacent 工具读取的 ini 都不得带 BOM；另 `GIT_CONFIG_COUNT` 注入时数值必须等于 KEY/VALUE 对数，多数少数 git 直接拒跑 [实证: 2026-09-11] | 2026-09-11 |
| M009 | 首编 dawn/tint 生成器报 `compile: version "go1.27.0" does not match go tool version "go1.25.0"` | dawn 用项目内 CIPD go（third_party/dawn/tools/golang/<plat>/bin/go，1.25），但系统环境变量 `GOROOT=D:\ohmyenv\go`（1.27）污染之：go 命令按 GOROOT 取 1.27 的 pkg/tool 编译器 | 原则：项目内工具链优先，构建 shell 净化 Go 环境（`GOROOT/GOPATH/GOCACHE` 置空）再跑 autoninja；chromium 生态自带 python/go，系统级 go/python 环境变量不得外泄进构建 [实证: 2026-09-11 清 GOROOT 后项目内 go 自洽] | 2026-09-11 |
| M010 | 临时测试实例用默认 user-data-dir 启动后 `Stop-Process` 清理，把用户手动开的浏览器整个连带杀掉 | chrome 单例（singleton）按 user-data-dir 合并：默认目录的新启动会并入已有默认目录实例，测试拿到的进程句柄实为用户实例主进程；杀它=杀全家 | 测试实例必须带独立 `--user-data-dir`（如 C:\tmp\bh-test）再启动，清理只杀该句柄；杀任何 chrome 前先核对目标实例的 user-data-dir 独立性 [实证: 2026-09-11 误杀用户现场后重启恢复] | 2026-09-11 |
| M011 | 源码内用「提前 return」短路安全检查，编译报 `error: code will never be executed [-Werror,-Wunreachable-code]` | Chromium 全树开 `-Wunreachable-code-aggressive` + `-Werror`，任何被短路成死码的分支都是硬错误，不是警告 | 短路整段逻辑用**替换式锚定**（把目标块整块换成注释），不用提前 return；插入式锚定只用于纯增量内容；补丁脚本因此需要 before/after/replace 三模式 [实证: 2026-09-11 两处返工后替换式一次过] | 2026-09-11 |
| M012 | 剔除 BrowserWindowFeatures 里的对象创建块（设想让成员保持空 unique_ptr），浏览器**启动即崩**（Crashpad 落 dmp） | 窗口装配成员并非处处可空：别处代码对成员直接解引用/工厂注册契约被破坏；「成员是 unique_ptr 所以可空」是未验证假设 | 动窗口装配/对象图前先全文件 grep 成员的所有使用点；抑制行为优先改**行为方法体**或**feature/pref 默认值**这类单一开关，不动对象创建；增量编译后必须先回归「能启动」再看行为 [实证: 2026-09-11 启动崩溃即回滚] | 2026-09-11 |
| M013 | 三个锚（OSCrypt/启动管理器/会话恢复）常量已加进补丁脚本，用户却仍看到横幅：**锚的 edit_file 调用从未写进 main()**，脚本「成功」输出掩盖了未执行 | 补丁脚本常量与调用分离，加锚只加常量不改调用时无任何报错；每锚 [ok]/[skip] 输出与常量数不一致也不显眼 | 加锚后必须核对：main() 里 edit_file 调用数 = 锚常量数 = 输出 [ok]+[skip] 行数；行为验收（用户肉眼/自动化）是唯一可信终验，编译通过不代表锚生效 [实证: 2026-09-11 幽灵锚导致三轮无效追凶] | 2026-09-11 |
| M014 | Windows 打包树分发到 linux/mac 后，远端各类 `PermissionError: [Errno 13]`：depot_tools 的 `dirmd`、`third_party/node/<plat>/bin/node` 先后拒绝执行 | Windows 无执行位语义，跨平台 tar 不保 POSIX x 位；git 检出文件记录 100755 mode 但 Windows 工作区重建后实为 0644；node 等 git 之外的 CIPD 资产连 mode 记录都没有 | 分发落地后按 git 记录全量恢复执行位：逐仓 `git ls-files -s \| awk '$1=="100755"{print $4}' \| xargs chmod u+x`；git 之外资产（CIPD 解包、node tarball）单独 `chmod -R u+x` 兜底；两类恢复都要做，只做其一必踩下一处 [实证: 2026-09-12 双机先后踩 dirmd 与 node] | 2026-09-12 |
| M015 | Windows 树分发到 linux/mac 后三连死：gn 报 `Could not find gn executable`（runhooks 装不了它）、devtools `node_modules` 缺 `@rollup/rollup-<plat>` 平台原生包、`third_party/node/node_modules` 缺 `fast-glob` | 平台资产天然分平台：gn/lld 等平台 CIPD 二进制只由 `gclient sync` 按本机平台安装（runhooks 只管 hook，装不了 CIPD 依赖）；两棵 node_modules（devtools-frontend 与 third_party/node）都是 Windows 平台产物，npm optional deps 按安装平台落盘 | 分发树在新平台必须先跑 `gclient sync --with_branch_heads --with_tags`（git 仓已钉同 tag 零拉取，只补平台 CIPD，几分钟）；node_modules 两棵分别处理：`third_party/node/node_modules` 删除后由 sync 重装，devtools 的用系统 node 管理器（fnm）npm 补平台原生包 `npm i --no-save --no-package-lock --ignore-scripts <pkg>@<pin>`，零全局写入 [实证: 2026-09-12 双机三连修后进 CXX 编译] | 2026-09-12 |
| M016 | linux 构建编到 29% 死于 `module 'google.protobuf.internal.api_implementation' has no attribute 'Type'`，后续连环缺 `_PROTOBUF_INTERNAL_PYTHON_EDITION_DEFAULTS` | 同为 6.33.0 但内容不同：树里 `third_party/protobuf/python` 是 Chromium 打过兼容补丁的 runtime（保留 `Type()` 供 checked-in 旧 pb2 调用），vpython venv 按 spec 装的是纯 upstream wheel（无这些符号）；pyproto/venv 混搭 import 即崩。单修一个文件不够，pyproto 的 runtime 整个是 wheel 版 | 用树里配套 runtime **全量覆盖**两处：各 venv 的 site-packages/google/protobuf（顶层+internal）与 out/Release/pyproto/google/protobuf，统一 import 源后直接跑失败 import 验证；Windows 当时未复现，归因未深究（不阻塞） [实证: 2026-09-12 覆盖后 `import ssl_error_assistant_pb2` 现场通过、编译滚过原崩点] | 2026-09-12 |
| M017 | 新加的 GCM registration 锚首次运行即 `[skip] already patched`，实际从未打过：marker 用了 `const char kDefaultRegistrationURL[] =`，这是**上游本来就有的字节**，幂等判据永久误判 | 幂等 marker 必须是"打完补丁后才存在的字节"；用上游已有片段当 marker，该锚永远被跳过且无任何报错（M013 幽灵锚的姊妹坑：一个漏写调用，一个误判已打） | 写锚时自检：marker 必须来自 NEW 的内容（.invalid 域名、clean-chrome 注释等改后特有字节），不得来自锚文本本身；加锚后核对输出 `[ok]` 数等于新增锚数 [实证: 2026-09-12 换 `.invalid` 域名做 marker 后 [ok] 正常出现] | 2026-09-12 |
| M018 | 根目录改名（unsafe-chrome 改 clean-chrome）后续编，siso 第一步即 `fork/exec C:/unsafe-chrome/depot_tools/bootstrap-.../python3.exe: The system cannot find the path specified`，而该文件在新路径下明明存在 | `gn gen` 生成物（build.ninja/toolchain.ninja 等）内嵌生成时刻的**绝对路径**（python3/vpython3 wrapper 等）；depot_tools 自身文本零引用旧路径，exe 也都在，纯粹是 ninja 文件里的旧路径字符串被执行期 fork/exec | 改名根目录后必须重跑 `gn gen out\<目录>`（args 照 R001 五节）刷新 ninja 生成物再续编；Siso 内容寻址增量不受影响，重编量小 [实证: 2026-09-12 重 gen 后续编] | 2026-09-12 |
| M019 | 重 gen 后构建在**两个互斥编码坑**间打摆：`set_appcontainer_acls` 崩于 `'utf-8' codec can't decode byte 0xcb`（读 icacls），剥掉 `PYTHONUTF8` 后又在 `json5_generator.py` 崩于 `'gbk' codec can't decode byte 0x9d`（读旧 gen 产物） | ① out\Release 的 ACL 含首编 grant 的 ACE，icacls 显示为本地化中文组名（cp936），`PYTHONUTF8=1`（agent 会话默认）下 utf-8 解码必炸；② blink json5 的 `_write_file_if_changed` 读**上次生成**的 utf-8 产物不带 encoding，`PYTHONUTF8` 剥掉后 cp936 解码必炸；首编无旧产物，两坑都躲过 | 构建保留 `PYTHONUTF8=1`（json5 等读旧产物安全），acls action 单独用无 PYTHONUTF8 的 shell 手跑一次预置 stamp（ninja 随后跳过它）；另记 autoninja 默认并发在 64GB 机上撞内存峰值（mojom pickle MemoryError），续编显式 `-j 16` [实证: 2026-09-12 三连环修后 -j16 编译滚起] | 2026-09-12 |
| M020 | apply-auto-allow.py 分发到 lan-mac 首个写入即 `TypeError: write_text() got an unexpected keyword argument 'newline'`，树停在半打状态 | `Path.write_text(newline=)` 是 **Python 3.10 才加的参数**；mac CLT 系统 python3 是 3.9.6（linux 与 Windows 侧均 3.11 无感）；跨平台脚本用了比最老目标机更新的 stdlib API | 跨平台分发脚本按**最老目标 python** 写：写文件用 `path.open("w", encoding="utf-8", newline="")` 显式句柄（3.9 可用）；半打树先 `git checkout -- .` 回滚再重打；重打后以 `[ok]×43` 与幂等 `[skip]×43` 双向核验 [实证: 2026-09-12 mac 43 ok / linux 43 skip] | 2026-09-12 |
| M021 | Dev 全编（symbol_level=1）被系统低内存杀任务；同机 Release（symbol_level=0）-j16 却稳定 | Dev 的调试符号让单编译进程内存显著高于 Release，叠加常驻浏览器（冒烟遗留 chrome.exe）后 64GB 撞低水位；harness 会杀后台编译任务止损 | 编译前先收非必要浏览器进程；Dev 全编降并发（-j12 实证稳）；被杀后直接重跑同命令，ninja/siso 增量自愈 [实证: 2026-09-13 收浏览器+降 j 后续编] | 2026-09-13 |
| M022 | 经 ssh 发 compound 命令含 `pkill -f "模式"`，整条命令秒死（exit 255 无输出），远端启动根本没执行；同串另一处 `/tmp/cc-verify` 字面量就是凶手 | `pkill -f` 匹配**整条 cmdline**：远端 `bash -c '<全命令>'` 的 cmdline 里只要任何位置含有与模式匹配的文本（如 user-data-dir 路径），就把自己的外壳杀掉，ssh 随之断连；`[y]` 防自匹配只挡得住模式自身那一份，挡不住命令里其他位置的同一字符串 | 远端杀进程用**进程名精确匹配** `pkill -x chrome`（不扫 cmdline）；或模式与命令内其他文本隔离（先 pkill 的独立 ssh 调用，再第二次 ssh 启动）[实证: 2026-09-13 拆调用+pkill -x 后连跑通] | 2026-09-13 |
| M023 | 新锚用了旧记忆的 `base::Environment::GetVar(name, &out)` 双参签名，编译即挂：152 已改为单参 `std::optional<std::string> GetVar(cstring_view)` | 凭旧文档/记忆写上游 API,没先读目标 tag 的实际头文件 | 写锚前先 `rg` 目标树里该 API 的**当前签名**再落字节（本例 base/environment.h:43）；编译器虽会拦,但一轮增量编译也是一轮时间 | 2026-09-14 |

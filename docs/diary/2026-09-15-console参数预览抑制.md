# 2026-09-15：console 参数预览抑制（D02-8）

## 当日主线

- 用户新需求:console.* 传参经 CDP 上报时强制不生成对象预览（用户给了完整技术规格:origin==ConsoleAPI 强制 generatePreview=false,不动 Runtime.evaluate,A/B 双脚本验收）;第 1 轮三裁定（异常一并关/全批对齐 D02-7 节奏/收官一笔 feat）
- 152 树源码实读定收口点:v8-console-message.cc reportToFrontend 两分支（kConsole wrapArguments :374 / kException wrapException :338）,用户点名的 fromValue 在 152 已不存在;首入 v8/ 树（v8 是嵌在 src/ 的独立 git 仓,.patch 的 v8 段路径带 v8/ 前缀,src 根 git apply 直接可用）
- **前提修正（第 2 轮裁定）**:裸 CDP 实测通道图推翻原始规格前提:152 的 preview 不调 accessor/Proxy 陷阱（A 本就过）,B 的开火通道是文本构建（V8ValueStringBuilder 的 @@toStringTag Get）且 stock 同样开火（非 CDP 差分,关它反造差分）;用户裁定照规格落 preview-off（实效=console 参数/未捕获异常不再急切序列化进协议 payload,数据卫生+上游回归免疫）,文本通道保持原样
- 实现:2 新锚（kConsole/kException 各一,替换式,注释带 S006 出处）,48 至 50 锚,33 至 34 文件;双形态等价（仓库外沙盒 git apply 34 文件字节一致）+ 幂等（三机 ok=0/skip=50）
- 验收矩阵四端一致全绿:本机 Dev / browse-rs 部署（沙箱态）/ lan-mac / lan-ubuntu,console.debug 与 console.table 事件 args 及 exceptionThrown 的 exception 全部 hasPreview=false 且 objectId 保留,evaluate 两态（1+1 与 generatePreview:true）不变;A=false,B=true 记录在案（文本通道,stock 一致）
- 三机同步:mac 增量 13 步 40.3s,ubuntu 50 步 20.8s;双机行为验收用树内 node v24.12.0 跑同探针逐项比对
- 顺带修 M027:browse-rs 部署目录重建抹掉 AppContainer ACE 致沙箱态 0x5 崩,deploy-release.py 内置 icacls 自愈 + 根目录授底

## 关键决策

- 文本通道不杀（第 2 轮）:反检测纪律是与 stock 行为对齐,无差分通道改掉反而是主动暴露;preview-off 同理页面可观测行为零变化,价值在数据卫生与上游回归免疫
- 收口点选 reportToFrontend 而非 InjectedScript::wrapObject:后者无 origin 信息,会在 evaluate 路径误伤;两分支替换式锚覆盖 console 全家族与 console.table（generatePreview 门自然失活）
- 页面行为探针一律裸 CDP（fetch /json/list -> WebSocket -> Runtime.evaluate 的 .mjs）,不用 bh:M024 实证 bh eval 跑在 Node daemon,console.* 是 Node 的 console（util.inspect 读 stack）,全是 Node 侧噪音

## 文档轨迹

S006 立项定稿至六态全勾（通道矩阵/两锚设计/四端验证节）;MISTAKES M024-M027;README 快速开始第 6 条+仓库结构 ps1 笔误清账+当前状态刷新;R001 八节 ACE 自愈注记+坑表 21;CHANGELOG 条;INDEX 补丁行 50 锚/S006/diary 行/M027;PRD D02-8 两轮裁定已交付;GOAL 锚点/进程/历史行;TODO 全勾;deploy-release.py 加 ACE 自愈;feat: 一笔提交推送

## 遗留

- 无阻塞遗留;.probe 探针脚本为临时产物不入仓（范式已沉淀进 S006 文字）

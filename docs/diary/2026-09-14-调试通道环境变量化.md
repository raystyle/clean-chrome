# 2026-09-14:调试通道环境变量化（D02-6）

## 当日主线

- 用户新需求:启动时自动初始调试通道形态,环境变量控制 port/pipe/both,默认 port;追问链一轮澄清后立项（PRD D02-6）
- S005 机制研究:pipe handler 上游原生支持,无对话框路径（直接 AttachClient,与 auto-allow 互不相扰）;Windows 句柄契约 `--remote-debugging-io-pipes=<读>,<写>`（AdoptPipes 收养,免碰 CRT fd 编号）;断线关闸（CloseBrowserSoon）;无效句柄自退不崩;ASCIIZ/CBOR 双协议
- 实现:4 新锚（两个 include + 通道 env 块 + pipe-if 扩展）+ 1 锚改造（端口默认加 clean_chrome_port 条件）,43 至 47 锚;迁移走"回滚单文件重打"（ok=6/skip=41）
- 首编译即挂 M023:GetVar 凭旧记忆写双参签名,152 已改单参 optional;修后增量重编过
- 验收矩阵三态全绿:unset 至 9222 照旧;pipe 至 管道 getTargets 往返+9222 不听;both 至 双通道;双形态沙盒字节等价;Dev 增量同批完成

## 关键决策

- env 变量名定 `CLEAN_CHROME_DEBUG`,值大小写不敏感,非法值回退 port;显式开关永远压过变量
- 落点全部在 remote_debugging_server.cc（43 锚边界内文件）,不扩文件面
- pipe-smoke.py 沉淀为 tools 一员:兼做验收工具与自动化启动器范本（Windows 句柄契约活样例）

## 文档轨迹

S005 立项定稿（六态全勾）;MISTAKES M023;README 快速开始补通道节;R001 六节补三态说明与安全注意;INDEX 补丁行 47 锚/tools pipe-smoke/研究节 S005/错误节 M023;CHANGELOG 条;PRD D02-6 已交付

## 遗留

- ~~双机同步 47 锚~~（见下方续章）
- ~~部署版刷新~~（见下方续章）

## 深夜续章：部署刷新 + 双机同步 + 换机

- browse-rs 刷新至 47 锚（499 文件;刷新前收掉占锁的部署实例,用户日常 Chrome Dev 不动）;管道验收:官方工具外壳在部署路径反复"无响应/卡壳",最终由同契约探针两次实证（getTargets 680 字节回包,浏览器存活）,根因链:旧阻塞读代码僵尸 + 硬杀后 profile Singleton 残留 + 无控制台 std 句柄改变启动器路径;pipe-smoke 加固（std 落盘/读线程+队列有界/收尾段异常吞噬/失败自动倒浏览器日志）
- lan-mac 47 锚:checkout 单文件+重打 ok=6/skip=41,增量 7 步 31 秒完成;验收四点（开关 grep=1/通道锚入源/unset 9222/pipe 无句柄自退,POSIX 走 abort 同为不留尸）
- **换机裁定（用户）:linux 编译机 lan-linux 换为 lan-ubuntu**（16 核/61GB/416G 空闲,同局域网 WiFi）;策略:linux 对 linux rsync 全量（含 out/,同路径 /home/ray/chromium 保 ninja 增量态）,~30GB 传输中;lan-ubuntu 免密通道打通（注入公钥）;rsync 完成后自动 47 锚迁移+增量+验收（巡检 cron 挂守）
- 修正早前误判:siso"缓存回放"实为冷编速度,不存在 out 外全局缓存（~/.cache 无 siso）

## 09-15 凌晨续:bad-flags 剔除与换机收官

- D02-7 立项即做:bad_flags_prompt.cc 守卫变量式锚（无死代码无 unused 风险）,48 锚 33 文件;本机截图目检 --no-sandbox 无黄条
- lan-ubuntu 收官:rsync 61.9GB 含 out/,同路径增量态保真,48 锚 ok=7/skip=41,增量仅 4 步 21.6s,首跑验收过（新机 linux 线就绪）
- lan-mac 48 锚:ok=1/skip=47,增量 6 步 28.9s
- browse-rs 部署目录现仍 47 锚（D02-6 时点）,下次 deploy 一并带 48

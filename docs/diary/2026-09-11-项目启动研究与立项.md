# 2026-09-11：项目启动研究与立项

> 一天一篇，概貌级。细节归 S001 / R001 / P0001 与 patches。

## 日记自省：收工总结

### 批次

- 批次一（日间）：上游核实、补丁双形态与回环验证
- 批次二（晚间）：project-evo 骨架、目录迁移（research 并入 poc 体系）、check.py 13/13、建仓两笔提交（0479f7f feat、f4af4b3 docs）并推送 `github.com/raystyle/unsafe-chrome` main

### 概貌

从「用户指南是否可信」起步，当日完成三块：上游核实（七个结论全部实证落 S001）、补丁双形态产出并回环验证（apply-auto-allow.ps1 与钉 152.0.7977.84 的 .patch，字节等价）、project-evo 骨架立项（六原语 + docs 六目录 + P0001 进行中）。构建未开始。

### 关键裁决（用户）

- 项目定位：自编译 Chromium 加开关，只用于本机自动化，不进上游、不替换日常 Chrome
- 仓库推送到 `github.com/raystyle/unsafe-chrome`
- 顶层 research 目录并入 docs 体系，参照产物落 `poc\S001-chromium152-auto-allow\`
- 研究阶段可用 bh 命令与 browser 技能

### 得

- 先测后交付：补丁脚本先在参照树上跑、diff 暴露两处 bug（签名重复、空行丢失）再修复，最终双形态回环验证 [实证: S001 第五节]
- 「官方文档原文优先」省掉三处想当然：DEPOT_TOOLS_WIN_TOOLCHAIN、enable_nacl 已删、SDK 精确版本号
- md-guard 挡板真实生效，首版 README 的 Unicode 箭头与长破折号当场清零

### 失与坑（明日带入）

- PowerShell here-string 首尾换行被吞导致两处返工（M001）；char 重载 Replace 抛非终止异常（M002）；git diff --no-index 反斜杠路径带引号（M003）；autocrlf 干扰补丁字节比对（M004）：全部已落 MISTAKES.md
- 明日动作：按 TODO 顺序推进构建（git 预检、VS 2026 安装起）

### 明日入口

GOAL 进程段「环境待办」四项；PLAN 步骤 0 至 1；TODO 表前两行。

---

## 批次三：迁移收尾、网络破局、首编与验收全绿（晚间）

### 战况

- E 盘故障平移 C: 后收尾：fetch 残骸归位 chromium\；VS 2026 修复补齐 + Debuggers 用 winsdksetup /features 补装（VS 组件图已无该包 ID）
- 网络强干扰全程作战：googlesource/git-https/SSH 大流量全被间歇掐（12 轮 fetch 全灭、SSH 1.3GB 死于 Bad packet）；破局链 = aria2x16 codeload tarball（103Mbps）+ GitHub SSH 归化官方 tag + gclient 循环与陪跑自愈（半成品仓自动删重拉）+ insteadOf 镜像注入三仓
- 环境坑当日四连修：pip.ini BOM 炸 venv（M008）、系统 GOROOT 污染 dawn 内置 go（M009）、Defender 未排除 21 文件/秒、-j32 OOM 降 j16
- 首编约 4.5 小时（j16,blink/v8 巨型 obj 段拖慢）,out\Dev 约 20GB
- 验收四条全绿：findstr 开关入 chrome.dll；/json/version 152;WS 握手 10ms 零对话框；bh 经 BH_CDP_URL 附着 js() 页面执行通

### 得

- 量化先于决策：tools\net-probe.py（uv 全平台）七通道采样判级,网络问题从玄学变工程
- 「下载与 git 化解耦」模式确立：tarball 落工作区、git 对象走 SSH 窗口归化,大仓获取不再被单一协议绑架
- 布局价值主张定档（R001 预检节）：稳定增量编译 + 环境快速稳定重建,换机半天可恢复

### 失与坑（带入）

- 停 fetch/gclient 后残骸三连坑（M006 扩 shallow.lock）;GIT_CONFIG_COUNT 数值必须等于键值对数（M008）
- bh daemon 缓存过期 browser UUID 反复连 404 误报 403;BH_CDP_URL 直指端点即绕（已反馈,属 bh 侧待修）
- R001「首编 1 至 2 小时」在 64GB RAM 机器偏乐观：j16 实测 4.5 小时,已回填校准

### 明日入口

TODO 余项：out\Release 分发产物全编;CHANGELOG 封版与否待用户定;E:\unsafe-chrome 旧副本清理待确认。
